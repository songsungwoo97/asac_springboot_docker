spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/asacdb?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 0412
  jpa:
    show-sql: true # Hibernate/JPA ? ?? ???? SQL?? ??? ?????? ????.
    hibernate:
      ddl-auto: create # ?? ?????? ??? ?? ? ?? ??? ????? ??
    properties:
      hibernate:
        format_sql: true # ?? sql ?? ?? ???? ??
        database-platform: org.hibernate.dialect.MySQL8InnoDBDialect
